#include "main.h"
/**
 * _abs-computes absolute valu
 * @a:the integer to compute its absolute value
 * Return:returns tha abs value
 */
int _abs(int a)
{
	int k = a, absvalue;

	if (k < 0)
	{
		k = k * (-1);
	}
	absvalue = k;
	return (absvalue)
}
